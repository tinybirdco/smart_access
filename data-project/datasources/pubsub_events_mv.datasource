# Data Source created from Pipe 'pubsub_mv'

SCHEMA >
    `datetime` DateTime,
    `customer_id` String,
    `installation_id` String,
    `access_point_id` String,
    `user_id` String,
    `user_name` String,
    `device_type` String,
    `device_id` String,
    `access_granted` UInt8

ENGINE "MergeTree"
ENGINE_PARTITION_KEY "toYYYYMM(datetime)"
ENGINE_SORTING_KEY "customer_id, installation_id, access_point_id, device_type, datetime"
